#ifndef MOCK_UI_H
#define MOCK_UI_H

/**
 * Runs a mock UI demonstration.
 * Cycles through different LCD display modes and views to test
 * the UI rendering system without requiring real sensor data.
 */
void mock_ui_run_lcd(void);

#endif
